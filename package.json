{
  "name": "skillstak-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "workspace/apps/*",
    "workspace/packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "check": "turbo run check"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}

